<%= form_for(@motivo_bono_guilty) do |f| %>
  <% if @motivo_bono_guilty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motivo_bono_guilty.errors.count, "error") %> prohibited this motivo_bono_guilty from being saved:</h2>

      <ul>
      <% @motivo_bono_guilty.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_motivo %><br />
    <%= f.text_field :id_motivo %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :evento %><br />
    <%= f.text_field :evento %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
