<%= form_for(@liga_super_sf4_general) do |f| %>
  <% if @liga_super_sf4_general.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@liga_super_sf4_general.errors.count, "error") %> prohibited this liga_super_sf4_general from being saved:</h2>

      <ul>
      <% @liga_super_sf4_general.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :personaje_principal %><br />
    <%= f.collection_select :personaje_principal, PersonajeSuperSf4.all, :nombre, :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
