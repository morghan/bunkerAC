<h1>Listing log_pelea_guilties</h1>

<table>
  <tr>
    <th>Id ganador</th>
    <th>Id perdedor</th>
    <th>Puntos ganador</th>
    <th>Puntos perdedor</th>
    <th>Fecha</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @log_pelea_guilties.each do |log_pelea_guilty| %>
  <tr>
    <td><%= log_pelea_guilty.id_ganador %></td>
    <td><%= log_pelea_guilty.id_perdedor %></td>
    <td><%= log_pelea_guilty.puntos_ganador %></td>
    <td><%= log_pelea_guilty.puntos_perdedor %></td>
    <td><%= log_pelea_guilty.fecha %></td>
    <td><%= link_to 'Show', log_pelea_guilty %></td>
    <td><%= link_to 'Edit', edit_log_pelea_guilty_path(log_pelea_guilty) %></td>
    <td><%= link_to 'Destroy', log_pelea_guilty, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Log pelea guilty', new_log_pelea_guilty_path %>
