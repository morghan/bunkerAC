<%= form_for(@liga_blaz_blue) do |f| %>
  <% if @liga_blaz_blue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@liga_blaz_blue.errors.count, "error") %> prohibited this liga_blaz_blue from being saved:</h2>

      <ul>
      <% @liga_blaz_blue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :personaje_principal %><br />
    <%= f.collection_select :personaje_principal, PersonajeBlazBlue.all, :nombre, :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
